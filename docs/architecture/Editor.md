# ZoneForge — World Editor Architecture

## What It Is

`zoneforge-editor` is a **standalone desktop application** built in Unity 2022.3 LTS (3D URP). It is not a Unity Editor tool — it is a compiled executable that designers run independently, like WoWEdit or StarEdit.

It connects to the same SpacetimeDB backend as the game client. Changes made in the editor (zones, tiles, entities) are immediately visible to all connected game clients in real time.

## Engine & Rendering

- **Unity 2022.3 LTS** with **Universal Render Pipeline (URP), 3D**
- Top-down / isometric camera perspective (orthographic)
- 3D world on the X/Z plane (Y = up)
- Target platforms: Windows, macOS, Linux

## Project Structure

```
Assets/
├── Scripts/
│   ├── Runtime/        ← MonoBehaviours and runtime managers
│   │   ├── SpacetimeDBManager.cs  ← singleton, handles connect/subscribe/tick
│   │   ├── ZoneEditorManager.cs   ← zone creation, tile painting, entity placement
│   │   └── TilePainter.cs         ← raycast-based 3D tile grid painting
│   ├── Data/           ← ScriptableObject definitions
│   │   ├── WorldData.cs
│   │   └── ZoneVisualData.cs
│   └── autogen/        ← Generated by `spacetime generate` — do not edit
└── Art/
    ├── Tiles/          ← 3D tile prefabs (one per tile type)
    ├── Sprites/        ← Entity thumbnail sprites for palette UI
    └── Prefabs/        ← Reusable prefabs (entity gizmos, UI widgets)
```

> `Assets/Scripts/Editor/` may exist for dev tools (placeholder generators, etc.) but is excluded from builds automatically.

## SpacetimeDB Integration

Identical to the game client:

**Connection lifecycle:**
1. `DbConnection.Builder()` — connect to `http://localhost:3000` (dev) or cloud URL (prod)
2. `OnConnect` — subscribe to zone, entity, and tile tables
3. `OnSubscriptionApplied` — register table event callbacks (`OnInsert`, `OnUpdate`, `OnDelete`)
4. `FrameTick()` — called every `Update()` to process incoming messages

**Generated bindings** — `spacetime generate --lang csharp` produces typed C# classes for every table and reducer. Output goes to `Assets/Scripts/autogen/`. Regenerate after any server schema change.

## UI Architecture

All UI is built with **Unity UI Toolkit (UIElements)** or **uGUI** — never IMGUI / EditorGUI (those are Editor-only and won't compile in builds).

Planned UI panels:

| Panel | Purpose |
|-------|---------|
| Zone browser | List and select existing zones from SpacetimeDB |
| Zone creation form | Name + dimensions → `create_zone` reducer call |
| Tile palette | Grid of available tile prefabs with preview thumbnails |
| Entity palette | Grid of entity types with thumbnail sprites |
| Tool bar | Active tool (paint, erase, select, entity place) |
| Properties panel | Selected entity/tile properties and metadata |

## 3D Tile Grid

Tiles are **3D GameObjects** placed on the X/Z plane, not 2D Tilemap tiles.

**Tile placement flow:**
1. Raycast from camera through mouse position to the grid plane (`new Plane(Vector3.up, 0)`)
2. Snap the world hit point to cell coordinates (integer X/Z)
3. Destroy any existing tile at that cell
4. Instantiate the selected tile prefab at the snapped position

**Grid representation:** A `Dictionary<Vector3Int, GameObject>` tracks placed tiles at runtime. This is synced to/from SpacetimeDB via tile placement reducers (Phase 2).

## Key Unity Packages

| Package | Purpose |
|---------|---------|
| Universal Render Pipeline (URP) | Core rendering pipeline |
| UI Toolkit | Runtime world-building UI panels |
| SpacetimeDB Unity SDK | Real-time backend integration |

## See Also

- [Overview.md](Overview.md) — System architecture and data flow
- [Client.md](Client.md) — Game client architecture (for comparison)
- [../guides/Editor_Dev.md](../guides/Editor_Dev.md) — Daily editor development workflow
- [../guides/Getting_Started.md](../guides/Getting_Started.md) — Full environment setup
